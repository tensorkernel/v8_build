name: Build V8 Android ARM64 Sandbox PoC

on:
  workflow_dispatch:
    inputs:
      v8_ref:
        description: "V8 branch, tag, or full commit SHA"
        required: true
        default: "main"
        type: string
  push:
    paths:
      - ".github/workflows/build-v8-android-arm64.yml"

permissions:
  contents: read

concurrency:
  group: v8-android-arm64-${{ github.ref }}
  cancel-in-progress: false

env:
  DEPOT_TOOLS: ${{ github.workspace }}/depot_tools
  SRC_ROOT: ${{ github.workspace }}/v8-work
  V8_DIR: ${{ github.workspace }}/v8-work/v8
  OUT_DIR: ${{ github.workspace }}/v8-work/v8/out.gn/android-arm64-sandbox-poc
  PACKAGE_ROOT: ${{ github.workspace }}/packages
  V8_REF: ${{ inputs.v8_ref || 'main' }}

jobs:
  build:
    name: Android ARM64 d8
    runs-on: ubuntu-24.04
    timeout-minutes: 360

    steps:
      - name: Show runner resources
        shell: bash
        run: |
          set -euxo pipefail
          uname -a
          nproc
          free -h
          df -h

      - name: Reclaim runner disk space
        shell: bash
        run: |
          set -euxo pipefail
          sudo rm -rf \
            /usr/local/lib/android \
            /usr/share/dotnet \
            /opt/ghc \
            /usr/local/.ghcup \
            /usr/local/share/boost \
            /usr/share/swift \
            /opt/hostedtoolcache/CodeQL || true
          docker system prune --all --force --volumes || true
          sudo apt-get clean
          df -h

      - name: Install bootstrap packages
        shell: bash
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            binutils \
            ca-certificates \
            curl \
            file \
            git \
            jq \
            lsb-release \
            ninja-build \
            pkg-config \
            python3 \
            python3-setuptools \
            unzip \
            xz-utils \
            zip \
            zstd

      - name: Install depot_tools
        shell: bash
        run: |
          set -euxo pipefail
          git clone --depth=1 \
            https://chromium.googlesource.com/chromium/tools/depot_tools.git \
            "$DEPOT_TOOLS"
          echo "$DEPOT_TOOLS" >> "$GITHUB_PATH"
          export PATH="$DEPOT_TOOLS:$PATH"
          gclient --version

      - name: Check out V8 and Android dependencies
        shell: bash
        run: |
          set -euxo pipefail
          export PATH="$DEPOT_TOOLS:$PATH"

          mkdir -p "$SRC_ROOT"
          git init "$V8_DIR"
          git -C "$V8_DIR" remote add origin \
            https://chromium.googlesource.com/v8/v8.git
          git -C "$V8_DIR" fetch --depth=1 origin "$V8_REF"
          git -C "$V8_DIR" checkout --detach FETCH_HEAD

          cat > "$SRC_ROOT/.gclient" <<'EOF'
          solutions = [
            {
              "name": "v8",
              "url": "https://chromium.googlesource.com/v8/v8.git",
              "deps_file": "DEPS",
              "managed": False,
              "custom_deps": {},
              "custom_vars": {},
            },
          ]
          target_os = ["android", "unix"]
          EOF

          cd "$SRC_ROOT"
          gclient sync --nohooks --no-history --shallow -D

          V8_COMMIT="$(git -C "$V8_DIR" rev-parse HEAD)"
          V8_SHORT_SHA="$(git -C "$V8_DIR" rev-parse --short=12 HEAD)"
          echo "V8_COMMIT=$V8_COMMIT" >> "$GITHUB_ENV"
          echo "V8_SHORT_SHA=$V8_SHORT_SHA" >> "$GITHUB_ENV"
          git -C "$V8_DIR" show -s --format=fuller HEAD
          df -h

      - name: Install V8 Linux and Android build dependencies
        shell: bash
        run: |
          set -euxo pipefail
          export PATH="$DEPOT_TOOLS:$PATH"
          cd "$V8_DIR"
          env DEBIAN_FRONTEND=noninteractive \
            ./build/install-build-deps.sh --no-prompt
          gclient runhooks
          df -h

      - name: Generate Android ARM64 sandbox build
        shell: bash
        run: |
          set -euxo pipefail
          export PATH="$DEPOT_TOOLS:$PATH"
          cd "$V8_DIR"
          mkdir -p "$OUT_DIR"

          cat > "$OUT_DIR/args.gn" <<'EOF'
          # Real Android ARM64 target, not an ARM64 simulator.
          target_os = "android"
          target_cpu = "arm64"
          v8_target_cpu = "arm64"

          # Release-like standalone d8 build with native symbols retained.
          is_component_build = false
          is_debug = false
          is_official_build = false
          dcheck_always_on = false
          symbol_level = 2

          # V8 sandbox and the official sandbox-testing corruption API.
          v8_enable_pointer_compression = true
          v8_enable_pointer_compression_shared_cage = true
          v8_enable_external_code_space = true
          v8_enable_sandbox = true
          v8_enable_memory_corruption_api = true

          # Send d8 output to stdout when running under Android/ADB.
          v8_android_log_stdout = true

          # Do not fail a security research build solely on new warnings.
          treat_warnings_as_errors = false
          EOF

          gn gen "$OUT_DIR" --fail-on-unused-args
          gn args "$OUT_DIR" --list --short | tee "$OUT_DIR/effective_gn_args.txt"

          grep -F 'target_os = "android"' "$OUT_DIR/effective_gn_args.txt"
          grep -F 'target_cpu = "arm64"' "$OUT_DIR/effective_gn_args.txt"
          grep -F 'v8_target_cpu = "arm64"' "$OUT_DIR/effective_gn_args.txt"
          grep -F 'is_debug = false' "$OUT_DIR/effective_gn_args.txt"
          grep -F 'dcheck_always_on = false' "$OUT_DIR/effective_gn_args.txt"
          grep -F 'v8_enable_sandbox = true' "$OUT_DIR/effective_gn_args.txt"
          grep -F 'v8_enable_memory_corruption_api = true' "$OUT_DIR/effective_gn_args.txt"

      - name: Build d8 with all runner CPUs
        shell: bash
        run: |
          set -euxo pipefail
          export PATH="$DEPOT_TOOLS:$PATH"
          cd "$V8_DIR"
          autoninja -C "$OUT_DIR" -j "$(nproc)" d8
          df -h

      - name: Verify binary and build configuration
        shell: bash
        run: |
          set -euxo pipefail
          export PATH="$DEPOT_TOOLS:$PATH"
          cd "$V8_DIR"

          test -x "$OUT_DIR/d8"
          test -f "$OUT_DIR/v8_build_config.json"

          file "$OUT_DIR/d8" | tee "$OUT_DIR/FILE.txt"
          readelf -h "$OUT_DIR/d8" | tee "$OUT_DIR/ELF_HEADER.txt"
          readelf -l "$OUT_DIR/d8" | tee "$OUT_DIR/PROGRAM_HEADERS.txt"

          grep -q 'AArch64' "$OUT_DIR/ELF_HEADER.txt"

          python3 - "$OUT_DIR/v8_build_config.json" <<'PY'
          import json
          import sys

          path = sys.argv[1]
          with open(path, "r", encoding="utf-8") as f:
              cfg = json.load(f)

          def enabled(key):
              value = cfg.get(key)
              return value is True or str(value).lower() == "true"

          expected_true = [
              "is_android",
              "sandbox",
              "memory_corruption_api",
              "pointer_compression",
              "pointer_compression_shared_cage",
          ]
          for key in expected_true:
              if not enabled(key):
                  raise SystemExit(f"Expected {key}=true, got {cfg.get(key)!r}")

          if cfg.get("arch") != "arm64":
              raise SystemExit(f"Expected arch=arm64, got {cfg.get('arch')!r}")
          if cfg.get("target_cpu") != "arm64":
              raise SystemExit(
                  f"Expected target_cpu=arm64, got {cfg.get('target_cpu')!r}"
              )
          if enabled("simulator_run"):
              raise SystemExit("This is unexpectedly a simulator build")
          if enabled("dcheck_always_on"):
              raise SystemExit("Primary build unexpectedly has DCHECKs always on")

          print(json.dumps(cfg, indent=2, sort_keys=True))
          PY

      - name: Package runtime and symbols
        shell: bash
        run: |
          set -euxo pipefail
          export PATH="$DEPOT_TOOLS:$PATH"
          cd "$V8_DIR"

          RUNTIME_DIR="$PACKAGE_ROOT/v8-android-arm64-sandbox-poc"
          SYMBOLS_DIR="$PACKAGE_ROOT/v8-android-arm64-sandbox-poc-symbols"
          mkdir -p "$RUNTIME_DIR" "$SYMBOLS_DIR"

          install -m 0755 "$OUT_DIR/d8" "$RUNTIME_DIR/d8"
          for name in icudtl.dat snapshot_blob.bin v8_build_config.json \
                      v8_features.json effective_gn_args.txt FILE.txt \
                      ELF_HEADER.txt PROGRAM_HEADERS.txt; do
            if [[ -f "$OUT_DIR/$name" ]]; then
              install -m 0644 "$OUT_DIR/$name" "$RUNTIME_DIR/$name"
            fi
          done
          install -m 0644 "$OUT_DIR/args.gn" "$RUNTIME_DIR/args.gn"

          if [[ -f "$OUT_DIR/exe.unstripped/d8" ]]; then
            install -m 0755 "$OUT_DIR/exe.unstripped/d8" \
              "$SYMBOLS_DIR/d8.unstripped"
          else
            echo "No exe.unstripped/d8 was produced" > "$SYMBOLS_DIR/NOTICE.txt"
          fi

          git rev-parse HEAD > "$RUNTIME_DIR/V8_COMMIT.txt"
          git show -s --format=fuller HEAD > "$RUNTIME_DIR/V8_COMMIT_DETAILS.txt"
          cp "$RUNTIME_DIR/V8_COMMIT.txt" "$SYMBOLS_DIR/V8_COMMIT.txt"
          cp "$OUT_DIR/args.gn" "$SYMBOLS_DIR/args.gn"

          gn desc "$OUT_DIR" //:d8 runtime_deps --all \
            > "$RUNTIME_DIR/runtime_deps_from_gn.txt" || true

          cat > "$RUNTIME_DIR/verify-sandbox.js" <<'EOF'
          'use strict';

          print('Sandbox API type: ' + typeof Sandbox);
          if (typeof Sandbox !== 'object') {
            throw new Error('Sandbox testing API is unavailable');
          }

          print('Sandbox base: 0x' + Sandbox.base.toString(16));
          const view = new Sandbox.MemoryView(0, 0x1000);
          print('Sandbox.MemoryView length: 0x' + view.byteLength.toString(16));
          print('Android ARM64 sandbox-testing build is ready.');
          EOF

          cat > "$RUNTIME_DIR/run-sandbox-poc.sh" <<'EOF'
          #!/system/bin/sh
          set -eu
          cd "$(dirname "$0")"
          exec ./d8 \
            --run-as-sandbox-security-poc \
            --sandbox-prohibit-insecure-mode \
            "$@"
          EOF
          chmod 0755 "$RUNTIME_DIR/run-sandbox-poc.sh"

          {
            echo "V8 Android ARM64 Sandbox PoC build"
            echo "V8 commit: $V8_COMMIT"
            echo "V8 ref requested: $V8_REF"
            echo "GitHub run: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
            echo "Runner: $RUNNER_NAME"
            echo "Runner OS: $RUNNER_OS $RUNNER_ARCH"
            echo "Build CPUs: $(nproc)"
            echo
            file "$OUT_DIR/d8"
            readelf -l "$OUT_DIR/d8" | grep -F 'Requesting program interpreter' || true
          } > "$RUNTIME_DIR/BUILD_INFO.txt"

          (
            cd "$RUNTIME_DIR"
            sha256sum * > SHA256SUMS.txt
          )
          (
            cd "$SYMBOLS_DIR"
            sha256sum * > SHA256SUMS.txt
          )

          cd "$PACKAGE_ROOT"
          tar --zstd -cf \
            "$GITHUB_WORKSPACE/v8-android-arm64-sandbox-poc-${V8_SHORT_SHA}.tar.zst" \
            "$(basename "$RUNTIME_DIR")"
          tar --zstd -cf \
            "$GITHUB_WORKSPACE/v8-android-arm64-sandbox-poc-symbols-${V8_SHORT_SHA}.tar.zst" \
            "$(basename "$SYMBOLS_DIR")"

          cd "$GITHUB_WORKSPACE"
          sha256sum \
            "v8-android-arm64-sandbox-poc-${V8_SHORT_SHA}.tar.zst" \
            "v8-android-arm64-sandbox-poc-symbols-${V8_SHORT_SHA}.tar.zst" \
            > ARTIFACT_SHA256SUMS.txt

          ls -lh *.tar.zst ARTIFACT_SHA256SUMS.txt

      - name: Upload d8 build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: v8-android-arm64-sandbox-poc-${{ env.V8_SHORT_SHA }}
          path: |
            v8-android-arm64-sandbox-poc-${{ env.V8_SHORT_SHA }}.tar.zst
            v8-android-arm64-sandbox-poc-symbols-${{ env.V8_SHORT_SHA }}.tar.zst
            ARTIFACT_SHA256SUMS.txt
          if-no-files-found: error
          compression-level: 0
          retention-days: 14

      - name: Write job summary
        shell: bash
        run: |
          {
            echo "## V8 Android ARM64 sandbox PoC build"
            echo
            echo "- Commit: \`$V8_COMMIT\`"
            echo "- Requested ref: \`$V8_REF\`"
            echo "- Target: \`android/arm64\`"
            echo "- Sandbox: enabled"
            echo "- Memory corruption API: enabled"
            echo "- DCHECK always on: disabled"
            echo "- Artifact: \`v8-android-arm64-sandbox-poc-$V8_SHORT_SHA\`"
            echo
            echo "The binary targets Android/Bionic and cannot be executed directly on the x64 GitHub runner."
          } >> "$GITHUB_STEP_SUMMARY"
